db {
    url = "jdbc:postgresql://localhost:5432/task_roulette"
    user = "main_user"
    password = "password"
    driver = "org.postgresql.Driver"
}
server {
    host = "0.0.0.0"
    port = 8080
}
